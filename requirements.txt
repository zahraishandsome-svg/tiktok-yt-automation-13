# Core
# Nightly, not stable: TikTok broke on 2026-08-14 and the fix only exists in
# yt-dlp nightlies (stable 2026.7.4 fails every TikTok video). --pre lets pip
# see .devN builds. update-ytdlp.yml keeps this pointed at the newest nightly.
--pre
yt-dlp>=2026.8.16.20253.dev0
# Browser-impersonation backend for yt-dlp — required so TikTok serves a parseable
# page (fixes 'Unable to extract universal data for rehydration'). Used by
# tiktok_downloader._inject_impersonate.
# MUST stay in yt-dlp's supported range: yt-dlp only registers curl_cffi
# 0.5.10 or 0.10.x–0.14.x (see yt_dlp/networking/_curlcffi.py). 0.15+ is rejected
# and silently yields ZERO impersonation targets, breaking TikTok fetches.
curl_cffi>=0.10,<0.15
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0
